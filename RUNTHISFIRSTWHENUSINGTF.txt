1. Install this
WINDOWS
winget install Hashicorp.Terraform

2. Add here yung token ng auth token mo po sa railway
WINDOWS
$env:RAILWAY_TOKEN="" 

3. Run
terraform init

4. Deploy
terraform apply -auto-approve

5. To Destroy
terraform destroy -auto-approve